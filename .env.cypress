APP_ENV=testing
APP_NAME="TaskManager Cypress"
APP_ENV=testing  # Importante para que o Laravel saiba que está em um ambiente de teste
APP_KEY=base64:83cU9pdX7CUoGYFtDa21SAxjyXArXVDXPm4OJdfQ/hc=  # Copie a APP_KEY do seu arquivo .env principal ou gere uma nova
APP_DEBUG=true
APP_URL=http://localhost:8000 # Ou a URL que o Cypress usa para acessar seu app

APP_LOCALE=pt_BR

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=taskmanager_test # << NOVO NOME PARA O BANCO DE DADOS DE TESTE
DB_USERNAME=root             # << Seu usuário do MySQL
DB_PASSWORD=                  # << Sua senha do MySQL (se houver)

# Configurações otimizadas para testes (opcional, mas recomendado)
SESSION_DRIVER=array
CACHE_STORE=array
QUEUE_CONNECTION=sync # Processa jobs imediatamente em vez de enfileirar

# Outras configurações podem ser copiadas do seu .env principal se necessário,
# mas as acima são as mais cruciais para o banco de dados e ambiente.
# Mantenha as configurações de e-mail, AWS, etc., como estão ou use drivers de log/array
# para evitar efeitos colaterais durante os testes.

MAIL_MAILER=log
